name: Android Build and Release

on:
  push:
    paths-ignore:
      - 'ios/**'
  workflow_dispatch: # Allows manual triggering from the Actions tab

jobs:
  build:
    name: Build Android APKs
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.0.0'
          cache: 'npm'

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '17' # Recommended for modern React Native/Gradle versions
          distribution: 'zulu'

      - name: Install Dependencies
        run: |
          corepack enable
          yarn config set nodeLinker node-modules
          yarn install --immutable
          ./node_modules/.bin/patch-package

      - name: Run Jetifier
        run: |
          # Find the jetify script anywhere inside the jetifier package folder
          JETIFY_BIN=$(find node_modules/jetifier -name "jetify" -type f | head -n 1)
          if [ -z "$JETIFY_BIN" ]; then
            echo "Error: Jetifier script not found in node_modules/jetifier"
            exit 1
          fi
          echo "Found jetifier at: $JETIFY_BIN"
          node "$JETIFY_BIN" -r

      - name: Change wrapper permissions
        run: chmod +x android/gradlew

      - name: Build DevDebug APK
        run: |
          cd android
          ./gradlew assembleDevDebug

      - name: Build Debug APK
        run: |
          cd android
          ./gradlew assembleDebug

      - name: Upload APKs to Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            android/app/build/outputs/apk/dev/debug/*.apk
            android/app/build/outputs/apk/debug/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
